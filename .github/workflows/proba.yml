# yaml-language-server: $schema=https://json.schemastore.org/github-workflow
name: proba

on: 
  workflow_dispatch:
  [push]:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Bahur
      run: echo bahur
